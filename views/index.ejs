<%- layout('layout.ejs') %>
<div>
    <h3><%- title %></h3>
    <p><a href="/create" class="btn btn-success">Add an Acitivity</a></p>
    <table class="table table-striped table-condensed">
        <tr>
            <th>Activity Name</th>
            <th>Date Created</th>
            <th></th>
        </tr>
        <% for(var i=0; i<data.length; i++) { %>
            <tr>
                <td><%= data[i].activity_name %></td>
                <td><%= data[i].date_created %></td>
                <td>
                    <a href="/edit/<%= data[i]._id %>" class="btn btn-primary btn-small">Edit</a>
                    <a href="/delete/<%= data[i]._id %>" class="btn btn-primary btn-small">Delete</a>
                </td>
            </tr>
        <% } %>
    </table>
</div>